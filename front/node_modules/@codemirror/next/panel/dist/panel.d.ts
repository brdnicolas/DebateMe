import { EditorView, ViewUpdate } from "../../view";
import { Facet, Extension } from "../../state";
export declare function panels(): Extension;
export interface Panel {
    dom: HTMLElement;
    mount?(): void;
    update?(update: ViewUpdate): void;
    style?: string;
    top?: boolean;
    pos?: number;
}
export declare const showPanel: Facet<(view: EditorView) => Panel, readonly ((view: EditorView) => Panel)[]>;
//# sourceMappingURL=panel.d.ts.map